# Variables que consume docker-compose.yml (raíz del proyecto).
# Para correr sin Docker, mirá BACKEND/.env.example y FRONTEND/.env.example.

DB_NAME=travelplannerbd
DB_PASSWORD=travelplanner

# Generalas con: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=
SECRET_KEY=

# Flujo de n8n (opcional)
N8N_WEBHOOK_URL=
N8N_TOKEN=
